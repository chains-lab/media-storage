type: array
items:
  type: object
  required:
    - exit
    - size
  properties:
    exit:
      type: string
      description: "exit name"
    size:
      type: integer
      format: int64
      description: "exit size"